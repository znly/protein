v1
    race tests
    benchmarks encoding/decoding vs. vanilla
    doc, comments, real world example (needs a note about errors) (needs a note about logging)
    runtime decoding of enums
    runtime decoding of oneofs
    expose metrics (follow rocksDB model?)
    expose logging (same place as metrics?)
    gogo.stdtypes support
